[
{% for host in groups[beegfs_ha_ansible_cluster_group] %}
  {
    "username": "{{ beegfs_ha_cluster_username }}",
    "token": "{{ host | to_uuid }}"
  }{% if not loop["last"] %},{% endif %}

{% endfor %}
]