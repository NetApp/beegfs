[defaults]
any_errors_fatal = True

[inventory]
#DEVNOTE (jmccormi): Ensure a missing or empty inventory file is treated as a failure as this could easily go unnoticed as Jenkins would not fail the build.
unparsed_is_failed = True
any_unparsed_is_failed = True